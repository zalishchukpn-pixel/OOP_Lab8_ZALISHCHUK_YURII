def stringify(node):
    result = []
    curr = node
    
    while curr is not None:
        result.append(str(curr.data))
        curr = curr.next
    
    result.append("None")
    
    return " -> ".join(result)
